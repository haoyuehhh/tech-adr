# ADR-003: 大模型推理服务三级熔断与成本管控设计

**状态**: 已接受 | **日期**: 2025-10-20 | **作者**: 季澔辰 | **相关项目**: 昆山数字创新谷-新闻推荐系统

---

## 背景

实习期间负责的推荐系统调用 12B 参数大模型做内容生成，高峰期 GPU 成本飙升且服务不稳定：

- 📊 P99 延迟 2000ms，触发下游超时
- 💾 GPU 显存占用 92%，偶发 OOM 导致服务重启
- 💰 单月 GPU 费用超预算 40%

---

## 决策

实施**"主模型 → 轻量模型 → Mock 数据"三级熔断降级策略**，并接入 ONNX Runtime 蒸馏模型作为常态降级层。

---

## 备选方案

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| 直接扩容 GPU | 简单无脑 | 成本线性增长，ROI 极差 | ❌ 否 |
| 纯缓存策略 | 命中时极快 | 长尾请求仍打爆 GPU，缓存失效雪崩 | ❌ 否 |
| **三级熔断+模型蒸馏** | 成本可控，体验分级保障 | 需维护 3 套模型，Mock 数据需定期更新 | ✅ **采纳** |

---

## 三级熔断设计

| 层级 | 触发条件 | 响应策略 | 用户体验 |
|---|---|---|---|
| **L1 主模型** | 负载 < 70%<br/>显存 < 80% | 12B 原始模型全量推理 | ⭐⭐⭐ 高质量生成 |
| **L2 轻量模型** | 负载 70-85% 或<br/>显存 80-90% | 切换 1.5B 蒸馏模型<br/>延迟 40ms | ⭐⭐ 可接受质量 |
| **L3 Mock 数据** | 负载 > 85% 或<br/>下游超时 | 预生成模板 + 热点关键词拼接 | ⭐ 保底不报错 |

---

## 影响评估

### ✅ 正面
- 月均 GPU 费用下降 ¥4,200
- 服务可用性：97.5% → 99.9%
- P99 延迟稳定在 150ms 以内

### ⚠️ 负面
- 1.5B 模型需每周用新数据蒸馏更新
- Mock 模板库需人工维护

### 🔧 技术债务
- 当前熔断阈值硬编码，未来需接入 Prometheus 动态阈值调整

---

## 验证

| 维度 | 数据 |
|---|---|
| **成本** | 2025 年 10 月 GPU 账单对比（脱敏截图可私发面试官） |
| **性能** | 12B → 1.5B：延迟 200ms → 40ms，显存占用 ↓68% |
| **代码** | 实习项目内部仓库（面试时可展示脱敏版核心逻辑） |

---

## 相关链接

- 🔗 [ONNX Runtime 优化笔记](链接) - 内部文档
- 📋 后续迭代 - 计划接入 vLLM 持续批处理（ADR-004 待写）
