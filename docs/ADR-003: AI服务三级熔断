# ADR-003: 大模型推理服务三级熔断与成本管控设计

- **状态**: 已接受
- **日期**: 2025-10-20
- **作者**: 季澔辰
- **相关项目**: 昆山数字创新谷-新闻推荐系统

## 背景
实习期间负责的推荐系统调用12B参数大模型做内容生成，高峰期GPU成本飙升且服务不稳定：
- P99延迟2000ms，触发下游超时
- GPU显存占用92%，偶发OOM导致服务重启
- 单月GPU费用超预算40%

## 决策
实施"主模型→轻量模型→Mock数据"三级熔断降级策略，并接入ONNX Runtime蒸馏模型作为常态降级层。

## 备选方案

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| 直接扩容GPU | 简单无脑 | 成本线性增长，ROI极差 | 否 |
| 纯缓存策略 | 命中时极快 | 长尾请求仍打爆GPU，缓存失效雪崩 | 否 |
| **三级熔断+模型蒸馏** | 成本可控，体验分级保障 | 需维护3套模型，Mock数据需定期更新 | **采纳** |

## 三级熔断设计

| 层级 | 触发条件 | 响应策略 | 用户体验 |
|---|---|---|---|
| L1 主模型 | 负载&lt;70%，显存&lt;80% | 12B原始模型全量推理 | 高质量生成 |
| L2 轻量模型 | 负载70-85%或显存80-90% | 切换1.5B蒸馏模型，延迟40ms | 可接受质量 |
| L3 Mock数据 | 负载&gt;85%或下游超时 | 返回预生成模板+热点关键词拼接 | 保底不报错 |

## 影响
- **正面**: 月均GPU费用下降4200元；服务可用性从97.5%提升至99.9%；P99延迟稳定在150ms以内
- **负面**: 1.5B模型需每周用新数据蒸馏更新；Mock模板库需人工维护
- **技术债务**: 当前熔断阈值硬编码，未来需接入Prometheus动态阈值调整

## 验证
- 成本数据: 2025年10月GPU账单对比（脱敏截图可私发面试官）
- 性能数据: 12B→1.5B，延迟200ms→40ms，显存占用↓68%
- 代码: 实习项目内部仓库（面试时可展示脱敏版核心逻辑）

## 相关链接
- ONNX Runtime优化笔记: [内部文档]
- 后续迭代: 计划接入vLLM持续批处理（ADR-004待写）
